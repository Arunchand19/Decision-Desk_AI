# Cancellations

An order can be cancelled before it ships. Request the order number when it is missing and verify the shipping status before confirming cancellation.

Orders already shipped cannot be cancelled; explain the return process instead.
